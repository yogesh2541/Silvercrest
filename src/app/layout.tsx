import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "@/components/Providers";
import Navbar from "@/components/common/Navbar";
import Footer from "@/components/common/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Silvercrest Commerce - Premium Furniture & Home Decor | India",
  description: "Silvercrest Commerce - Premium Furniture and Home Decor Ecommerce. Shop luxury furniture, elegant designs, and premium home accessories across India. Registered under Companies Act, 2013.",
  keywords: "furniture, home decor, premium furniture, luxury furniture, furniture online, home furniture india, interior design, sofas, beds, dining sets",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col" style={{backgroundColor: '#F8F5F0'}}>
        <Providers>
          <Navbar />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </Providers>
      </body>
    </html>
  );
}
